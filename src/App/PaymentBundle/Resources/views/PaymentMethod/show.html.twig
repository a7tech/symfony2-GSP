{% extends 'AppPaymentBundle:PaymentMethod:layout.html.twig' %}

{% set nav_title = 'Payment method overview' %}

{% block actions_main %}

    <table class="table table-striped">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ entity.name }}</td>
        </tr>
        </tbody>
    </table>

{% endblock %}
